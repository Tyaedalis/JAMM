#include <iostream>

namespace JAMM
{
    class MemoryManager
    {
        public:
            MemoryManager(); // Default constructor

            int Initialize();
            int Shutdown();
        private:

    };
}